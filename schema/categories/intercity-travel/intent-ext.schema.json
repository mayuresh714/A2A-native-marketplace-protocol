{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://a2a-marketplace.dev/schema/v0/categories/intercity-travel/intent-ext.schema.json",
  "title": "intercity_travel Intent preferences (v0)",
  "description": "The category-specific soft preferences for an intercity travel Intent — validated against Intent.preferences (TIER 3). Used only for ranking and coalition compatibility, never for grouping.",
  "type": "object",
  "properties": {
    "exact_destination": { "$ref": "https://a2a-marketplace.dev/schema/v0/common.schema.json#/$defs/geoPoint", "description": "Precise drop point within the destination_region grouping key." },
    "detour_tolerance_km": { "type": "number", "minimum": 0 },
    "comfort_prefs": {
      "type": "array",
      "items": {
        "enum": ["female_co_passengers_only", "no_smoking", "quiet_ride", "ac_required"]
      },
      "description": "Soft comfort constraints used in coalition compatibility (docs/design.md §6). Rejections based on these are never disclosed to the rejected party."
    },
    "provider_min_rating": { "type": "number", "minimum": 0, "maximum": 5 }
  },
  "additionalProperties": false
}
