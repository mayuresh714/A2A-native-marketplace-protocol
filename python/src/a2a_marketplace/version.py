"""Library and protocol version.

``PROTOCOL_VERSION`` is the wire-contract version (must match the ``schema/``
envelope const). ``__version__`` is the library's own release version; they
may diverge as the library iterates on a stable protocol.
"""
PROTOCOL_VERSION = "0.1.0"
__version__ = "0.1.0"
